info:
  name: Check a Job
  type: http
  seq: 4

http:
  method: GET
  url: http://127.0.0.1:8080/api/job/{{job_id}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
