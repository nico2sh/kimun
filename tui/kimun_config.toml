# ─── Kimün configuration ────────────────────────────────────────────────────
#
# KEY BINDINGS
# ────────────
# Supported combinations:
#   - ctrl and/or alt (with optional shift) + a letter (a-z)
#   - bare F-key (F1–F12, no modifier required)
# Any combo that does not follow these rules is silently ignored when loaded.
#
# Format per action:
#   ActionName = ["<modifiers> & <letter>", ...]
#
# Available modifiers (combine with +):  ctrl   alt   shift
#
# Examples:
#   Quit         = ["ctrl&Q"]            # Ctrl+Q
#   SearchNotes  = ["ctrl&E"]            # Ctrl+E
#   OpenNote     = ["ctrl&O"]            # Ctrl+O  (fuzzy file finder)
#   OpenSettings = ["ctrl+shift&P"]      # Ctrl+Shift+P
#   NewJournal   = ["ctrl&J"]            # Ctrl+J
#   FileOperations = ["F2"]              # F2  (open file-ops menu: delete/rename/move)
#
# ─────────────────────────────────────────────────────────────────────────────
config_version = 2
last_paths = ["/journal/2026-03-19.md", "/journal/2026-01-07.md", "/note.md", "/pep.md", "/something.md", "/index.md", "/journal/2026-03-23.md", "/journal/2026-03-24.md", "/journal/2025-12-22.md", "/journal/2025-11-03.md", "/journal/2024-10-28.md", "/journal/2025-06-13.md", "/journal/2024-10-30.md", "/journal/2025-12-12.md", "/journal/2026-03-18.md", "/journal/2026-03-27.md", "/newnote.md", "/note_in_root.md"]
theme = ""
autosave_interval_secs = 5
use_nerd_fonts = true
default_sort_field = "name"
default_sort_order = "ascending"
journal_sort_field = "name"
journal_sort_order = "descending"

[global]
current_workspace = "default"
theme = "Nord"

[workspaces.default]
path = "/Users/nhormazabal/Documents/Notes"
last_paths = ["/journal/2021-06-11.md", "/journal/2021-06-16.md", "/journal/2021-06-22.md", "/index.md", "/pep.md", "/somer.md", "/journal/2023-11-08.md", "/note in the root.md", "/newnote.md", "/note_in_root.md", "/journal/2026-03-20.md", "/journal/2026-03-23.md", "/journal/2026-03-24.md"]
created = "2026-03-27T06:54:12.989155Z"

[key_bindings]
Quit = ["ctrl&Q"]
OpenSettings = ["ctrl&P"]
ToggleNoteBrowser = ["ctrl&F"]
SearchNotes = ["ctrl&E"]
OpenNote = ["ctrl&O"]
NewJournal = ["ctrl&J"]
TogglePreview = ["ctrl&Y"]
TextEditor-Italic = ["ctrl&I"]
TextEditor-Image = ["ctrl+shift&L"]
TextEditor-ToggleHeader = ["ctrl&T"]
TextEditor-Underline = ["ctrl&U"]
TextEditor-Strikethrough = ["ctrl&S"]
ToggleSidebar = ["ctrl&B"]
FocusEditor = ["ctrl&L"]
FocusSidebar = ["ctrl&H"]
SortByName = ["ctrl&N"]
SortByTitle = ["ctrl&G"]
SortReverseOrder = ["ctrl&R"]
FileOperations = ["F2"]
