# 2026-04-08

## Standup

- Yesterday: Started caching layer, had auth flow meeting with [[maria]]
- Today: Continue caching implementation, write integration tests
- Blockers: None

## Auth Meeting Notes

Met with [[maria]] about OAuth2 PKCE. Key decisions:
- We'll support both authorization code flow and PKCE
- Token refresh will be handled by the SDK, not the server
- Need to update the [[api-docs]] with the new endpoints
- Timeline: 2 weeks for implementation, 1 week for testing

## Caching Design

Using Redis with a 5-minute TTL for search results. Cache key: `search:{workspace_id}:{query_hash}`.

Invalidation strategy: invalidate on note create/update/delete via pub/sub channel.

See [[search-caching]] for the full design doc.

## EOD

Pushed the first draft of the caching layer. Tests passing locally. Will need [[carlos]] to review the Redis integration.
